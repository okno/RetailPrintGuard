# Optional legacy sources. Values are passed as single path arguments, never
# evaluated as shell code. The canonical RetailPrintGuard spool is always used
# with --canonical-root; legacy sources remain explicit and read-only.
RPG_RCH_LEGACY_ROOT=
RPG_PRINTPROXY_LEGACY_ROOT=
RPG_PRINTPROXY_HMAC_KEY_FILE=
